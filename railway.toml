[build]
builder = "NIXPACKS"
installCommand = "npx --yes pnpm install --no-frozen-lockfile"
buildCommand = "npx --yes pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/db run push"

[deploy]
startCommand = "pnpm --filter @workspace/api-server run start"
